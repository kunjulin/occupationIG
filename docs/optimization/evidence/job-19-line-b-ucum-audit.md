# JOB-19 線 B｜UCUM 建議單位稽核紀錄

> 稽核日 2026-07-30｜方向：**以值集為真**｜對象：`input/assets/extended-ucum-reference.csv`（271 列）
> 稽核工具：`scripts/audit-ucum.js`（tx 於本容器被封鎖，以 CI 為 `$lookup` oracle）

## 1. 方法

對 CSV 之每個 LOINC 代碼以 `$lookup?property=*` 取官方 `EXAMPLE_UCUM_UNITS`，
與 `ucum_suggested` 逐筆四態比對。CSV 之 `ucum_suggested` 與官方單位皆可能為多值
（以 `;` 分隔），故兩側均切成集合後以子集判定，不可拿整串比對（見 §3）。

## 2. 四態分類結果（CI run 30525243319，tx.fhir.org/r4）

| 四態 | 筆數 | 說明 |
|:--|--:|:--|
| 相符 | **197** | `ucum_suggested` 之單位皆為官方 `EXAMPLE_UCUM_UNITS` 所列 |
| 不符 | **0** | — |
| LOINC 未提供 | **74** | 官方無 `EXAMPLE_UCUM_UNITS`（多為定性／影像／鏡檢項目） |
| 待人工判定 | **0** | 無「官方多值而 CSV 空白／擇一」之情形 |

CSV `verification` 欄由 100% 「需覆核」改為上述實測結果（197 相符、74 LOINC 未提供）。

## 3. Round 1 之比對邏輯修正（重要）

Round 1 之 informational 稽核回報 **4 筆「不符」**：`10334-1`、`14957-5`、`1869-7`、`24108-3`。
逐筆檢視發現此四筆之 `ucum_suggested` 與官方 `EXAMPLE_UCUM_UNITS` **字串完全相同**
（如 `10334-1`：兩側皆為 `[arb'U];[arb'U]/mL`），係比對邏輯將「CSV 多值整串」對「官方切分後之清單」
比對所致之**偽陽性**。修正 `classify()` 使 CSV 側亦切成集合後以子集判定，四筆歸為「相符」，
genuine 不符為 **0**。修正後之單元測試涵蓋此四筆及負向案例（`BOGUS/x`、`{pack}/d` → 不符）。

## 4. 閘門化（§2.3）

`build-ig.yml` 之 UCUM 步驟改為 `node scripts/audit-ucum.js --gate --max 0`：
任一列之 `ucum_suggested` 出現官方清單外之單位（不符 > 0）即建置失敗。

**核心紀律（§3.1／§5）**：「不符」一律列出供人工判定，腳本**不自動覆寫** CSV。
吸菸量 `64218-1` 之 `{pack}/d`（實為 `/d`）為「代碼選錯」而非「單位填錯」之基準案例
（已於 JOB-18 處理；該碼屬社會史，不在本 Extended 量值 CSV 內）。

## 5. 負向測試

因 tx 於本容器被封鎖，負向以 `classify()` 之單元測試證明機制：
`classify("BOGUS/x", ["mg/dL"]) → 不符`、`classify("{pack}/d", ["/d"]) → 不符`，
且閘門決策 `不符(1) > max(0) → exit 1`。CI 中 `--gate --max 0` 即據此對真實 tx 結果攔阻：
任何人將 `ucum_suggested` 改為官方清單外之單位，`UCUM 稽核閘門` 步驟即失敗。

## 6. 結論（⚠️ 已被 §7–§11 補篇修正，勿單獨引用）

`extended-ucum-reference.csv` 之 197 個有值列，其建議單位**全數與官方 LOINC `EXAMPLE_UCUM_UNITS` 相符**；
原「需覆核」之全域標記係過度保守，經本次程序確認後去除。UCUM 一致性自此納入 CI 閘門，
與 display 語意（JOB-01）、跨術語對映一致性（線 A）同為建置階段之自動攔阻項。

> ⚠️ **本節之涵蓋範圍陳述已被修正**：上述「197 列全數相符」僅對**當時對照檔之 271 列**成立，
> 而該檔凍結於 Extended 尚為 292 碼時期，另有 44 個值集內之量值碼從未被稽核。
> 詳見 §7–§11（補充事項 §2 施作）。**本節不得單獨引用為「Extended 量值項已全數驗證」之依據。**

---

# 補篇｜補充事項 §2／§3 施作後之重新稽核（2026-07-30）

## 7. 為何原「需覆核 0」不代表值集全部量值項已驗證

原 `audit-ucum.js` **以對照檔之 271 列為稽核全集**。該檔凍結於 `VS-ExtendedDataset`
尚為 292 碼之時期，其後 JOB-01／JOB-14 之換碼與回收從未回寫。故現存於值集之部分
量值碼**根本不在對照檔內、從未被 `$lookup`**，而閘門只讀對照檔，對其完全盲目。
「需覆核 0」僅證明那 271 列內部一致——**分母錯了**。此與 JOB-17 之
`loinc-valuesets.xlsx` 同型：手工維護之衍生資產與值集脫鉤，閘門只驗資產自身一致性。

## 8. 修正：全集由值集展開（沿用 JOB-17 模式）

稽核全集改為 `VS-ExtendedDataset` ∪ `VS-CoreDataset` ∪ `VS-TWHAVitalSigns` ∪
`VS-CoreUploadSet`（社會史碼）去重 **320 碼**，新增狀態「**未列於對照檔**」並納入閘門
（`--max-missing 0`），使下一次換碼之缺漏於建置階段自動被發現。

### 8.1 過程中之自我錯誤（記錄以免重犯）

首輪（CI run 30533847567）回報 **320 碼全數「不適用」而閘門空過**。原因為本人判準錯誤：
tx 之 `$lookup` 於 `SCALE_TYP` 以 `valueCoding` 回傳時，其 `code` 為 LOINC 答案清單碼
（Qn = `LP7753-9`），非字面 `"Qn"`，而 `pickProps` 原優先取 `code`。**若未核對分類筆數，
該輪會以「全綠」通過而實際上一碼未稽核**——與本 JOB 所要防範者同型。
修正：`valueCoding` 改優先取 `display`，並加 `normScale()` 以答案清單碼回推。

## 9. 重新稽核結果（CI run 30534387613，320 碼）

| 狀態 | 筆數 |
|:--|--:|
| 相符 | 182 |
| 不符 | **0** |
| LOINC 未提供 | 5 |
| 待人工判定 | 0 |
| **未列於對照檔** | **44** ← 涵蓋缺口，閘門如期失敗（exit 1） |
| 不適用（非量值型，主動排除並記錄 Scale） | 89 |
| Scale 未知（未對映之答案清單碼） | 29 |
| 查詢失敗 | 0 |

該輪閘門輸出 `✖ 未列於對照檔：44 筆 > 基準 0`——**此即本閘門之實地負向驗證**：
新機制確實攔下了舊機制看不見的缺口。

## 10. 回填之 44 碼及其官方建議單位

`mg/dL`：`13457-7`／`13458-5`／`1558-6`／`18262-6`／`2085-9`／`2089-1`／`2093-3`／`2160-0`／
`2339-0`／`2345-7`／`2571-8`／`3043-7`／`38483-4`　｜　`U/L`：`1743-4`／`30239-8`
`/uL`：`51478-6`／`51479-4`／`51480-2`／`51483-6`／`51484-4`／`51486-9`／`51487-7`／`798-9`／`87926-2`
`mm[Hg]`：`8462-4`／`8480-6`　｜　`[lb_av];kg`：`29463-7`／`3141-9`　｜　`[in_us];cm;m`：`8302-2`／`3137-7`
`umol/L`：`13965-9`　｜　`%`：`19252-6`　｜　`g/dL`：`2888-6`　｜　`kg/m2`：`39156-5`
`ng/mL`：`53962-7`　｜　`mg/L`：`5650-7`　｜　`ug/L`：`5684-6`　｜　`[IU]/L`：`63557-3`
`d;wk;mo;a`：`63632-4`　｜　**`/d`：`64218-1`**（吸菸量，補充事項 §3 之案例，現已納入閘門）
`ug/dL`：`77307-7`　｜　`cm`：`8280-0`　｜　`ug/mg{creat}`：`9318-7`
`35200-5`：官方無 `EXAMPLE_UCUM_UNITS`，標「LOINC 未提供」。

另刪除 **8 筆對照檔冗列**（CSV 有、值集無，皆為 JOB-01 換掉之錯碼）：
`13705-9`、`14390-9`、`14409-7`、`1783-0`、`19199-9`、`20627-6`、`46986-6`、`70028-6`。

對照檔由 271 列 → **307 列**（相符 233 ／ LOINC 未提供 74 ／ 需覆核 **0**）。

## 11. Scale 未知 29 筆之處置（未臆測）

`LP32888-7`（17）／`LP436123-6`（10）／`LP7747-1`（2）三個答案清單碼之正式 Scale 名稱
未經權威查證，**故不逕行納入亦不默默排除**——列為具名狀態並以實測值 29 為閘門上限
（`--max-unknown 29`），新增即失敗；判定事項登記為 [open-issues T-12](../../input/pagecontent/open-issues.md)。
其中 `LP436123-6` 之項目多為試紙／效價且部分確有官方單位（`5804-0` 為 `mg/dL`），
研判可能為 SemiQn，但此係由項目性質推得，非查證結果。**保留 29 不是結論，是待辦。**

---

# 續篇｜JOB-20：T-12 結案（Scale part 查證與分類失效自我檢查，2026-07-30）

## 12. 三個 part 代碼之官方 Scale（loinc.org 查證）

| Part 代碼 | 官方 Part Name | 筆數 | 判定 |
|:--|:--|--:|:--|
| `LP32888-7` | `Doc`（文件型） | 17 | 非量值型 → 明確排除 |
| `LP436123-6` | `SemiQn`（半定量型，2023-07-05 建立） | 10 | **量值型 → 納入稽核** |
| `LP7747-1` | `-`（多重；panel 成員以不同方式報告） | 2 | 非量值型 → 明確排除 |

§11 對 `LP436123-6` 之研判（疑為 SemiQn）經官方確認為正確；當時「未經查證即不納入」之處置亦正確。
`SemiQn` 建立於 2023-07-05，屬較新之 Scale 值，此即原對映表未涵蓋之原因。

## 13. 10 筆 SemiQn 之官方建議單位（CI run 30539459829 之 tx `$lookup`）

| LOINC | 官方 `EXAMPLE_UCUM_UNITS` | 狀態 |
|:--|:--|:--|
| `11218-5` | `mg/dL` | 相符 |
| `20621-9` | （官方未提供） | LOINC 未提供 |
| `30004-6` | `mg/dL` | 相符 |
| `31147-2` | `{titer}` | 相符 |
| `50560-2` | `[pH]` | 相符 |
| `5792-7` | `mg/dL` | 相符 |
| `5797-6` | `mg/dL` | 相符 |
| `5803-2` | `[pH]` | 相符 |
| `5804-0` | `mg/dL` | 相符（本次補列） |
| `9633-9` | `{titer}` | 相符 |

其中 9 筆已在對照檔且此前已比對相符，僅 `5804-0` 缺漏而補列（故對照檔 307 → **308** 列，
非 JOB-20 §4.5 預估之約 317——該預估假設 10 筆皆須補列）。
`20621-9` 官方未提供單位，標「LOINC 未提供」，屬**已知情形而非未決**。

## 14. 稽核結果（CI run 30539459829，320 碼）

| 狀態 | 筆數 |
|:--|--:|
| 相符 | 234 |
| 不符 | **0** |
| LOINC 未提供 | 7 |
| 待人工判定 | 0 |
| 未列於對照檔 | **0** |
| 不適用（`Ord` 53／`Doc` 17／`Nom` 7／`-` 2） | 79 |
| **Scale 未知** | **0** ← T-12 結案，`--max-unknown` 歸零 |
| 查詢失敗 | 0 |

Scale 分布已無任何原始 LP 碼：`Qn` 231／`Ord` 53／`Doc` 17／`SemiQn` 10／`Nom` 7／`-` 2 ＝ 320。

## 15. 分類失效自我檢查（JOB-20 §5）

**門檻**：`--min-inscope-ratio 0.5`，且「納入比對之量值碼 ＝ 0」時無條件失敗。
實測 **241／320 ＝ 75.3%**，餘裕約 25 個百分點。

**為何設 50% 而非貼近實測值**：本檢查是*塌陷偵測*，精確不變量已由三道零基準
（不符／未列於對照檔／Scale 未知）把關。門檻若設在 70% 以上，日後值集合理新增
約 20 個定性／影像碼即會拉低比率而誤報，而誤報只會誘使後人調高門檻，反而失去意義。
真正的判準失效不是掉幾個百分點——首輪即為 0／320（0%）。故取 50%：對合理組成變動
不敏感，對判準塌陷必然觸發。

**驗證**（單元測試）：`0/320 → 失敗`、`150/320（46.9%）→ 失敗`、`160/320（50.0%）→ 通過`、
`231/320（JOB-19，72.2%）→ 通過`、`241/320（本次，75.3%）→ 通過`。
**此檢查會攔下 §8.1 所記之首輪錯誤**，不必再靠人工核對分類筆數。
