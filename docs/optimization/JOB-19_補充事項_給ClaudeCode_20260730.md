# JOB-19 完成後之補充事項（3 項）

> 對象：Claude Code（repo `kunjulin/occupationIG`）
> 基準：main `63a8bfb`（PR #20 合併）／gh-pages `04dcac0`（publish run 30530772720）
> 提出日期：2026-07-30　｜　提出者：主持人（文件端交叉核對）

---

## 0. 先說結論

JOB-19 之執行方向與紀律皆正確——**方向未做反**（值集未動、`VS-ExtendedDataset` 維持 288）、
**`VERIFIED` 之紀律修正到位**（33 筆全改「待覆核」，且理由正確：SNOMED 側之
`$validate-code` 僅涵蓋 4 個社會史代碼，不含本表之檢驗對映）。

**負向測試之處理亦可接受**：以 `classify()` 單元測試（`BOGUS/x`、`{pack}/d` → `不符`）
加上閘門決策（`不符 > max → exit 1`）證明偵測能力，不需為此多跑兩輪 CI 推一份
故意壞掉的 CSV。**無須補做。**

以下 3 項為文件端交叉核對後發現之殘留問題，其中第 2 項會影響 JOB-19 之驗收結論。

---

## 1. `terminology.md` 之 UCUM 統計為舊數字

`input/pagecontent/terminology.md` 第 243–244 行：

```
| **LOINC 有提供建議單位** | **209** |
| LOINC 未提供建議單位（多為定性／影像／鏡檢項目） | 83 |
```

`209 + 83 = 292` —— 此為 Extended 尚為 292 碼時期之統計。JOB-19 實測為
**相符 197 ／ LOINC 未提供 74 ／ 合計 271**。

同一份文件之第 75 行已引用新機制（`audit-ucum.js --gate`），第 243 行卻仍為舊統計，
兩處自相矛盾。

**請更正該表，並註明統計基準（`extended-ucum-reference.csv` 之筆數，以及該檔目前未涵蓋之範圍——見第 2 項）。**

---

## 2. ⚠️ `extended-ucum-reference.csv` 未隨值集更新，稽核分母錯誤

**這一項會改變 JOB-19 之驗收結論，請優先處理。**

JOB-19 報告 UCUM 四態為「相符 197 / 不符 0 / LOINC 未提供 74 / 待人工判定 0」，
分母 271。惟 `VS-ExtendedDataset` 現有 **288 碼**，經比對有 **25 碼不在該 CSV**，
其中 **20 碼為量值型項目**：

| 代碼 | 項目 | 來源 |
|:--|:--|:--|
| `51480-2` `51486-9` `51484-4` `87926-2` `51483-6` `798-9` `51487-7` `51478-6` `51479-4` | 尿沉渣自動計數 9 碼（每體積） | JOB-01 批 3 換入 |
| `1743-4` | ALT with P-5'-P | JOB-01 批 1 換入（← `14390-9`） |
| `30239-8` | AST with P-5'-P | JOB-01 批 1 換入（← `14409-7`） |
| `13458-5` | VLDL-C by calculation | JOB-01 批 1 換入（← `46986-6`） |
| `9318-7` | ACR 隨機尿 | JOB-01 批 1 換入（← `13705-9`） |
| `5778-6` | Color of Urine | JOB-01 批 1 換入（← `20627-6`） |
| `77307-7` | 血中鉛（靜脈血） | Wave 9 改為 Preferred |
| `5684-6` | 尿錳（質量濃度） | JOB-01 批 3 加碼 |
| `5650-7` | 尿氟（質量濃度） | JOB-01 批 3 加碼 |
| `53962-7` | AFP（腫瘤標記型） | JOB-01 批 2 加碼 |
| `13965-9` | 同半胱胺酸（莫耳濃度） | JOB-01 批 2 加碼 |
| `19252-6` | 巨核細胞 | JOB-01 批 3 換入（← `70028-6`） |

（另 5 碼為定性項目 `3349-8`、`3879-4`、`3390-2`、`12327-3`、`14267-9`，
本質無單位，不影響。）

**即：JOB-19 稽核了舊碼集。** 該 CSV 建立於 292 碼時期，其後 JOB-01／JOB-14
之換碼與回收從未回寫該檔，故：

- `需覆核 0` 之結論，僅對 271 筆成立，**不代表 Extended 全部量值項已驗證**
- 上開 20 碼**目前無任何官方單位對照**，且 `audit-ucum.js --gate` **不會發現**
  （它只讀 CSV，CSV 沒有的碼就不在檢查範圍）

**這與 JOB-17 之缺陷同型**：手工維護之衍生資產未隨值集更新，而閘門只驗資產本身之內部一致性，
不驗資產是否涵蓋值集全集。

### 請辦事項

1. **`audit-ucum.js` 之稽核對象改為由值集展開**，而非讀取既有 CSV 之列。
   即：以 `VS-ExtendedDataset`（及後續納入之其他值集）之 Scale = Qn 代碼為全集，
   逐碼 `$lookup` 取 `EXAMPLE_UCUM_UNITS`，再與 CSV 比對。
2. **新增第五態 `未列於對照檔`**，並將其納入閘門：
   值集中之量值碼若不在 CSV，即為缺漏，`> 0` 應失敗（或至少列為具名類別追蹤）。
3. 補齊上開 20 碼之官方建議單位至 CSV。
4. **驗收**：`err = 0`、`Wrong Display Name = 0`、`VS-ExtendedDataset` = 288、
   且 **CSV 之筆數 ≥ 值集中量值碼之筆數**、`未列於對照檔 = 0`。

> ⚠️ 請勿以「把 20 碼加進 CSV」了事。**根因是 CSV 為手工維護且與值集脫鉤**，
> 若不改為由值集展開，下一次換碼仍會漏。JOB-17 對 `loinc-valuesets.xlsx`
> 已採「由值集自動產生 ＋ CI 攔阻」之模式，此處請沿用同一模式。

---

## 3. UCUM 閘門不涵蓋 Core 之生理量測與社會史代碼

經核對，下列 Core 層代碼**全部不在** `extended-ucum-reference.csv`：

```
8302-2  3137-7  29463-7  3141-9  8280-0  8480-6  8462-4  85354-9
72166-2  64218-1  63632-4
```

即吸菸量 `64218-1` 之 `{pack}/d` → `/d` 更正，**係由 JOB-18 以 `terminology.md`
文件註記處理，非由閘門保證**。同類錯誤若再發生於 Core 層，現行閘門抓不到。

### 請辦事項

於第 2 項改為「由值集展開」後，將稽核範圍一併擴及
`VS-CoreDataset`、`VS-TWHAVitalSigns`，以及社會史所引用之代碼。
若因該等代碼多為 panel 或 Coded 型而不適用 UCUM，請於腳本中明確排除並註明判準
（例如僅稽核 `Scale = Qn` 且 Property 非 `Type`／`Prid` 者），
**而非以「不在 CSV」被動排除**。

---

## 4. 文件端已同步之事項（供對照，無須處理）

| 交付文件 | 版本 | 已反映之 JOB |
|:--|:--|:--|
| 《IG 完整編碼附件》 | **v7.4** | JOB-14／15／18／19；跨術語對照 33 筆全改「待覆核」；UCUM 欄依稽核結果更新（190 相符值、73 LOINC 未提供、**20 標「⚠ 未納入稽核範圍」**） |
| 《勞工健檢上傳欄位建議修訂案》 | **v2.1** | JOB-18（`3137-7`／`3141-9` 補回、`64218-1` 單位 `/d`） |
| 結案報告 | v1.4 | JOB-15／16（eGFR 公式沿革、T-5 結案） |

附件 v7.4 之說明分頁已載明第 2、3 項之涵蓋缺口，並註明「已回報執行團隊」。
故本補充事項完成後，請告知，以便同步更新附件之該段敘述。

---

## 5. 交給 Claude 規劃用提示（可直接複製）

```
請閱讀 docs/optimization/ 下之 JOB-19，以及本補充事項（JOB-19_補充事項_給ClaudeCode_20260730.md），
就其中第 1、2、3 項產出實作計畫。

要求：
1. 第 2 項為主要工作，且會改變 JOB-19 之驗收結論。請先在計畫中回答一個問題：
   「JOB-19 報告『需覆核 0』，為什麼這個結論不代表 Extended 全部量值項已驗證？」
   答不出來就不要動手。
2. 修法不是「把 20 碼補進 CSV」。根因是 CSV 手工維護、與值集脫鉤。
   請說明你將如何把 audit-ucum.js 改為「由值集展開為全集」，並沿用 JOB-17 對
   loinc-valuesets.xlsx 所採之「自動產生 ＋ CI 攔阻」模式。若計畫只是補資料，即為理解錯誤。
3. 新增第五態「未列於對照檔」並納入閘門或具名類別。請說明你如何確保
   下一次換碼時該缺漏會被自動發現。
4. 第 3 項請說明你的排除判準（哪些代碼本質不適用 UCUM），
   並強調是「主動排除並註明」而非「不在 CSV 就跳過」。
5. 第 1 項為文字更正，請一併處理，並註明統計基準與涵蓋範圍。
6. 驗收：err = 0、Wrong Display Name = 0、VS-ExtendedDataset = 288、
   未列於對照檔 = 0、CSV 筆數 ≥ 值集中量值碼筆數。
7. 完成後回報：commit、CI run id、上述數值、以及補齊之 20 碼各自之官方建議單位。
```
